<p id="notice"><%= notice %></p>

<h1>Listing Shares</h1>

<table>
  <thead>
    <tr>
      <th>Isin</th>
      <th>Name</th>
      <th>Currency</th>
      <th>Stock exchange</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @shares.each do |share| %>
      <tr>
        <td><%= share.isin %></td>
        <td><%= share.name %></td>
        <td><%= share.currency %></td>
        <td><%= link_to share.stock_exchange.name, share.stock_exchange %></td>
        <td><%= link_to 'Show', share %></td>
        <td><%= link_to 'Edit', edit_share_path(share) %></td>
        <td><%= link_to 'Destroy', share, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br />

<div id="shares_chart_container"></div>

<br />

<div id="shares_chart_container_compare"></div>

<br />

<%= link_to 'New Share', new_share_path %>
